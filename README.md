# Sustania - We Are Sustainability

## Install

To install run
```
npm install
```

Now copy and rename `.env-example` to `.env`. Add your secrets to 
the `.env` file. 

Create a new database using the sql found in `products.sql` 

To run project run
```
npm run start 
```

Now go to `http://localhost:3001/`

